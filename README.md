# Prompt Keeper

一个 [SillyTavern](https://github.com/SillyTavern/SillyTavern) 插件，用于按聊天会话保存和恢复 Prompt Manager 条目状态（启用/禁用 + 排序）及当前活跃预设。

## 功能特性

- **按聊天独立配置** — 每个聊天会话记住自己的预设条目启用/禁用状态和排列顺序
- **预设联动** — 保存时记录当前使用的预设名称，恢复时自动切换到对应预设
- **手动保存** — 通过界面按钮手动保存，避免误操作
- **自动恢复** — 切换聊天时自动恢复已保存的配置（带防抖机制）
- **脏检查** — 恢复前对比当前状态与保存状态，避免不必要的重复操作
- **集成 UI** — 保存/恢复/删除按钮直接注入到设置面板中
- **状态指示** — 可视化显示当前聊天是否存在已保存的配置
- **免刷新热重载** — 更新插件文件且扩展入口被重新执行时会自动加载新版代码，也可通过 `window.PromptKeeperReload()` 手动热重载，通常无需刷新页面或重启后端

## 工作原理

当你点击保存按钮时，插件会读取 Prompt Manager 中所有条目的启用/禁用状态、排列顺序以及当前激活的预设名称，并将这些信息存储到当前聊天的 `chatMetadata` 中。

切换到其他聊天时，插件检测 `CHAT_CHANGED` 事件，经过防抖延迟后自动恢复之前保存的配置——包括切换到对应预设并还原条目状态。

### 数据存储

数据存储在聊天元数据 `chatMetadata.promptKeeperState` 中：

```json
{
    "prompts": {
        "main": true,
        "jailbreak": true,
        "persona_description": false
    },
    "promptOrder": [...],
    "presetName": "My Preset",
    "savedAt": 1718451600000
}
```

### 读取策略

插件优先通过 API 层（`chatCompletionSettings`）读取和写入条目状态，如果 API 不可用则回退到 DOM 操作。

## 安装

### 方法一：通过 SillyTavern 扩展安装器

1. 打开 SillyTavern
2. 进入 **扩展面板** → **安装扩展**
3. 粘贴仓库地址：
   ```
   https://github.com/sisjzknxhnsnejxn-cmyk/prompt-keeper
   ```
4. 点击 **安装**
5. 安装完成后首次启用通常需要刷新一次浏览器页面；后续更新如果扩展管理器重新执行入口会自动热重载，也可在控制台执行 `window.PromptKeeperReload()` 手动热重载，通常无需刷新页面或重启 SillyTavern 后端

### 方法二：手动安装

1. 进入 SillyTavern 安装目录
2. 导航到 `data/<user-handle>/extensions/`
3. 克隆本仓库：
   ```bash
   git clone https://github.com/sisjzknxhnsnejxn-cmyk/prompt-keeper
   ```
4. 首次安装后刷新浏览器页面；后续手动更新文件后，如果扩展入口被重新执行会自动热重载，也可在控制台执行 `window.PromptKeeperReload()` 手动热重载。如扩展列表未刷新，再重启 SillyTavern

## 使用方法

### 操作按钮（位于设置面板中）

| 按钮 | 功能 |
|------|------|
| 💾 **保存** | 保存当前预设条目的启用状态、排序和预设名称 |
| ↺ **恢复** | 手动恢复已保存的配置（包括切换预设） |
| 🗑 **删除** | 删除当前聊天的保存配置 |

### 插件设置（扩展设置面板）

| 选项 | 说明 |
|------|------|
| **启用插件** | 控制插件整体开关 |
| **切换聊天时自动恢复** | 开启后切换聊天会自动恢复配置，关闭则需手动恢复 |
| **保存时允许自定义名称** | 开启后同一聊天可保存多个不同槽位，最多 5 个 |

### 状态指示器

- `✓ 已保存` — 当前聊天存在已保存的预设条目配置
- `⚠ 无保存` — 当前聊天没有保存的配置

### 自动恢复机制

开启自动恢复后，切换聊天时插件会：

1. 等待 1.5 秒防抖延迟（避免快速切换时重复触发）
2. 执行脏检查，对比当前状态与保存状态
3. 如果保存了不同的预设，先切换预设
4. 等待预设加载完成后，恢复条目启用/禁用状态和排序

## 管理范围

Prompt Keeper **管理**：
- Prompt 条目的启用/禁用状态
- Prompt 条目的排列顺序
- 当前使用的预设名称
- 保存数据所在聊天的 `chatMetadata.promptKeeperState`

**不管理**：
- 模型选择
- API 设置
- Temperature / Top P
- Instruct 预设
- 上下文模板
- 生成参数

## 系统要求

- SillyTavern 1.12.0 或更高版本（需支持 Prompt Manager）

## 更新日志

### v2.1.0

- 新增免刷新热重载：更新插件文件且扩展入口被重新执行时会自动加载新版拆分脚本，也可在浏览器控制台执行 `window.PromptKeeperReload()` 手动热重载，通常无需刷新页面或重启 SillyTavern 后端
- 增强热重载清理逻辑：重载前会解绑旧事件、停止旧观察器并重新注入设置/UI，避免新旧运行时叠加
- 改进角色卡删除后重新导入场景的排序恢复：当 SillyTavern 重新生成 `character_id` 时，会尝试按 Prompt 条目 identifier 匹配兼容的 `prompt_order`
- 增强保存按钮防重复触发保护，降低触控设备或浏览器事件重复派发导致的连续保存风险
- 优化保存/恢复/删除按钮的触控事件绑定，并补充键盘 Enter / Space 操作支持
- 优化槽位弹窗的元信息展示，明确区分槽位名称、来源预设与保存时间
- 将设置页使用说明改为折叠区域，减少扩展设置面板占用空间
- 统一版本号到 `2.1.0` 并整理保存逻辑结构，提升维护可读性

### v2.0.3

- 修复部分 SillyTavern 版本在扩展界面更新/刷新后可能找不到插件或设置面板不出现的问题
- 为 `manifest.json` 补充稳定的 `name` / `id` 字段，降低仓库名、文件夹名、显示名不一致导致的识别失败概率
- 增强拆分脚本加载器：支持更稳的扩展路径推断、加载超时提示、缓存刷新与 loading/loaded 状态保护
- 说明：Prompt Keeper 是纯 JS/CSS 扩展，**不需要 HTML 文件**；没有 HTML 不是插件消失的原因

### v2.0.2

- 优化 Prompt Keeper 操作栏在手机、平板、PC 多端页面中的响应式布局，减少窄屏挤压和文字混淆
- 增强 iOS Safari / Android 浏览器的安全区、动态视口和触控目标适配
- 优化槽位选择弹窗宽度、高度、滚动和长名称换行表现，提升横屏与小屏可读性
- 为主要操作按钮补充更明确的无障碍标签，降低跨端交互误解

### v2.0.1

- 增强按钮事件绑定兼容性，改善 iOS Safari、Edge 与 SillyTavern 1.16 下点击被拦截或重复触发的问题
- 为槽位弹窗加入明暗主题切换与触控反馈
- 优化 UI 被 SillyTavern 刷新移除后的自动重新注入策略

### v1.1.1

- 修复脏检查在 DOM 回退路径下因 promptOrder 类型不一致（string[] vs object[]）导致永远判脏、触发不必要恢复的问题
- 清理冗余注释，精简代码

### v1.0.0

- 初始发布
- 按聊天保存预设条目状态（启用/禁用 + 排序）
- 保存并恢复活跃预设名称
- 切换聊天时自动恢复（带防抖 + 脏检查）
- 集成 UI：保存/恢复/删除按钮
- 状态指示器
- API 层优先读写，DOM 回退兜底
- MutationObserver 监控 UI 移除并自动重新注入

## 常见问题

**Q: 数据保存在哪里？**
A: 保存在当前聊天的 `chatMetadata.promptKeeperState` 中，跟随聊天文件一起存储。

**Q: 导出聊天备份后删除角色卡，再重新导入角色卡和聊天文件，还能恢复原来的预设吗？**
A: 通常可以，但取决于导入内容是否完整：

1. 聊天导出/导入必须保留 `chatMetadata.promptKeeperState`；
2. 目标设备/当前酒馆里必须存在同名 Chat Completion 预设，否则插件无法自动切换到原预设；
3. 预设中的 Prompt 条目 identifier 需要保持一致，插件才能恢复启用/禁用和排序；
4. 如果重新导入角色卡后 SillyTavern 给角色分配了新的 `character_id`，本插件会尝试按 Prompt 条目 identifier 匹配当前可用的 `prompt_order` 来恢复排序。

如果预设被改名、没有导入，或 Prompt 条目 identifier 已变化，插件仍会保留聊天里的保存数据，但恢复时可能提示“切换预设失败”或“已跳过缺失条目”。

**Q: 删除聊天后配置会怎样？**
A: 配置随聊天一起被删除，无需手动清理。

**Q: 会影响其他用户或角色吗？**
A: 不会。配置绑定到聊天 ID，每个聊天会话独立。

**Q: 会和其他扩展冲突吗？**
A: Prompt Keeper 使用事件委托和非侵入式 DOM 操作，不修改 SillyTavern 核心文件，冲突可能性很低。

**Q: 为什么恢复时提示某些条目被跳过？**
A: 如果保存时存在的条目在当前预设中不存在（比如切换了预设后条目列表不同），这些条目会被跳过并提示。

**Q: 可以关闭自动恢复吗？**
A: 可以。在扩展设置面板中取消勾选「切换聊天时自动恢复」即可，之后需要手动点击恢复按钮。

**Q: 为什么更新后扩展界面里偶尔找不到 Prompt Keeper？是因为没有 HTML 吗？**
A: 不是。SillyTavern 扩展可以只有 `manifest.json` + JS/CSS，HTML 并不是必需文件。偶发找不到通常和扩展更新器的名称匹配、安装目录名、浏览器缓存或前端热刷新有关。v2.0.3 起已补充稳定的 `name/id` 字段并增强拆分脚本加载器。若仍异常，建议确认安装目录名为 `prompt-keeper`；v2.1.0 起，如果扩展入口被重新执行会自动热重载最新插件文件，也可在浏览器控制台执行 `window.PromptKeeperReload()` 手动热重载，只有热重载失败时再强制刷新浏览器页面。

## 许可证

MIT License — 详见 [LICENSE](LICENSE)。
